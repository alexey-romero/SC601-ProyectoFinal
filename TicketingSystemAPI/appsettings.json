{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        // change parameters based on localhost connection
        "PostgreSQLDatabase": "Server=localhost;Database=ticketing_system;User=postgres;Password=;"
    }
}
